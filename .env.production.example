# =============================================================================
# WavDash Production Environment
# =============================================================================
# Copy this file, fill in real values, and store as the PROD_ENV_FILE GitHub secret.
# This file is written to /opt/wavdash/.env on the VPS during deploy.

COMPOSE_PROJECT_NAME=wavdash

# =============================================================================
# Laravel Application
# =============================================================================
APP_NAME=Wavdash
APP_ENV=production
APP_DEBUG=false
APP_KEY=              # Generate with: php artisan key:generate --show
APP_URL=https://app.wavdash.com
MARKETING_URL=https://wavdash.com

# =============================================================================
# Database (MySQL in Docker)
# =============================================================================
DB_DATABASE=wavdash
DB_USERNAME=wavdash
DB_PASSWORD=          # Use a strong password

# =============================================================================
# Session (cross-subdomain cookies)
# =============================================================================
SESSION_DOMAIN=.wavdash.com

# =============================================================================
# Cloudflare R2 Storage
# =============================================================================
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=          # https://<account-id>.r2.cloudflarestorage.com
R2_PRIVATE_BUCKET=audio-private
R2_PUBLIC_BUCKET=audio-public
R2_PUBLIC_URL=        # https://pub-<hash>.r2.dev

# =============================================================================
# Laravel Reverb (WebSockets)
# =============================================================================
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST=ws.wavdash.com
REVERB_PORT=443
REVERB_SCHEME=https

# =============================================================================
# CORS
# =============================================================================
CORS_ALLOWED_ORIGINS=https://wavdash.com

# =============================================================================
# Admin Panel
# =============================================================================
ADMIN_ALLOWED_IPS=127.0.0.1  # Add your IP: 1.2.3.4,127.0.0.1
SUPER_USER_NAME=Super Administrator
SUPER_USER_EMAIL=admin@wavdash.com
SUPER_USER_PASSWORD=          # Use a strong password

# =============================================================================
# Mail (log-only for demo, no real SMTP)
# =============================================================================
MAIL_FROM_ADDRESS=hello@wavdash.com
